server:
    port: 9999

spring:
  application:
    name: gateway
  cloud:
    gateway:
      routes:
      - id: basic
        uri: 'http://localhost:8200'
        predicates:
          - Path=/location/**